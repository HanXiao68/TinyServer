## 半同步半反应堆线程池
### 线程池
---

public:
        构造器（线程池中线程的数量，最多等待的连接请求数量）    
        析构器    
        append()添加请求连接。

private: 
woker() 工作线程运行的函数，不断从工作队列中取出任务，并执行。    
run( )

变量：线程池中线程数  
        请求队列中允许的最大请求数 
       
 ---
### 半同步/半反应堆线程池原理

---

主线程接受工作任务，并插入到工作队列中，睡眠在工作队列上的线程通过竞争获取执行权。
实现了主线程和工作线程的解耦。


---

### 线程池的作用

---
* 空间换时间，用服务器的资源来换取运行时间效率的减少。
* 池是一组相同类型的资源的集合，这组资源在服务器启动的时候，就别创建好。称为静态资源。
* 当服务器进入正式运行时，开始处理客户的请求时，如果需要相关的资源，可以直接从池中获取，无需动态分配。
* 服务器使用完一个线程之后，将这个线程放回线程池中，不用执行系统调用来释放资源。
